import { Route } from "@/interfaces/route";

export const routes: Route[] = [
    { path: '/projects', label: 'Projekti' },
    { path: '/developers', label: 'Attistītāji' },
    { path: '/about', label: 'Par mums' },
  ];